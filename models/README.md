Place shared model checkpoints for the indoor competition workspace here when a
module expects them at the project level.

Examples:

- `depth_anything_v2_metric_small.pth`
- other shared vision checkpoints if we decide to standardize them here
