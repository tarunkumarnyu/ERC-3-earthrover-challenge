Place MBRA / LogoNav deployment checkpoints here.

Expected files for the provided deployment scripts:

- `logonav.pth`

Optional additional files used by training-time depth modules:

- `depthest_ploss/encoder.pth`
- `depthest_ploss/depth.pth`
